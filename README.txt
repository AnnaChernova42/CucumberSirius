Команда запуска
mvn clean install -Dcucumber.options="--tags @testcase_test"


Последовательность запуска смоука
1. Терминал FDK. Чиповая карта. Регион указан
изменить настройки в виста ридере chip = true (в локальных сценариях ClientType = 2)
заполнить в файле postCSALogin.do.18 <atmRegion> и <person><region>
            	<atmRegion>
            		<id>72</id>
            		<name>Мурманская область г Мурманск</name>
            		<guid>4445000</guid>
            	</atmRegion>
            		<region>
            			<id>01</id>
            			<name>Регион #1</name>
            			<guid>4445230</guid>
            		</region>
mvn clean install -Dcucumber.options="--tags @8_buttons --tags @smoke --tags ~@region0 --tags ~@NOTchip"

2. Терминал FDK. Магнитная карта. Регион не указан
изменить настройки в виста ридере chip = false (в локальных сценариях ClientType = 1)
в файле postCSALogin.do.18 <atmRegion> и <person><region> НЕсуществующими регионами.
перезапустить томкат.
mvn clean install -Dcucumber.options="--tags @8_buttons --tags @smoke --tags @NOTchip"

3. Терминал TOUCH. Магнитная карта. Регион указан
Если запущена 1 АТМ, то
заполнить в файле postCSALogin.do.18 <atmRegion> и <person><region> существующими регионами
изменить настройки в terminal-settings на TOUCH
перезапустить томкат.
mvn clean install -Dcucumber.options="--tags @TOUCH --tags @smoke --tags @NOTchip"

4. Терминал TOUCH. Чиповая карта. Регион указан
Если запущена 1 АТМ, то изменить настройки в виста ридере chip=true
перезапустить томкат.
mvn clean install -Dcucumber.options="--tags @TOUCH --tags @smoke --tags ~@NOTchip"





1. заполнить в файле postCSALogin.do.18 <atmRegion> и <person><region> существующими регионами. Терминал FDK, чиповая карта
mvn clean install -Dcucumber.options="--tags @8_buttons --tags @high --tags ~@region0 --tags ~@NOTchip"
2. закоментирорвать в файле postCSALogin.do.18 <atmRegion> и <person><region>. Терминал FDK. чиповая карта
mvn clean install -Dcucumber.options="--tags @8_buttons --tags @high --tags @region0 --tags ~@NOTchip"
3. mvn clean install -Dcucumber.options="--tags @TOUCH --tags @high --tags @NOTchip"

Структура и требования
    Подробнее в гугле: язык gherkin
    Файл должен иметь расширение .feature и находиться в папке Features
    Первой строкой файла : #language: ru
    Файл начинается с названия фичи (Функционал: ...)
    Фича содержит сценарии, которые и являются тестами (Сценарий: ...)
    Сценарий содержит шаги, начинающиеся со слов Дано (предусловие), Когда (действия), Тогда (результат), И (дополнительные шаги в рамках одного этапа - любого из предыдущих)
    Файл со сценариями должен иметь кодировку UTF-8

Запуск тестов
    mvn clean install -Dcucumber.options="--tags @all_steps_touch_rus_example --tags ~@ignored"
    Можно хранить папку с тестами на общей шаре, чтобы не перебрасывать их между машинами по мере создания тестов.
    Для этого нужно в src\test\java\siriustest\run\RunnerTest.java изменить значение features: features = "\\\\1.1.1.1\\Features"

Отчеты
    1. Кратко, понятно, с результатами шагов:
    	target/reports-pretty/index.html
    Примечание: при при каждом запуске тестов этот отчет перезатирается

    2. Красиво, с крутыми графиками и процентными показателями:
    	Reports/.../cucumber-html-reports/overview-features.html
    Примечание: ну очень крутой отчет :)

Теги
    Тесты нужно помечать тегами, их может быть несколько. При этом можно помечать тегами как фичи так и отдельные сценарии в них.
    Предлагаемые теги:

    	@smoke - смоуки
    	@high - высокий приоритет ( короткий регрес)
    	@old - устаревшие
    Пример 1: фича помечена тегами @all_tests и @ignored - при запуске всех тестов (тег @all_tests) эта фича не будет запущена.
    Пример 2: фича помечена тегом @all_tests, но один сценарий в ней - тегом @ignored. В этом случае будут выполнены все сценарии кроме него.
    Пример 3: отдельный сценарий внутри фичи помечен тегом @test, при исполнении команды mvn clean install -Dcucumber.options="--tags @test" будет выполнен только один сценарий. По существу, эта роль отводится тегу @new_test.
    Теги можно создавать самостоятельно, даже указанные выше теги не являются обязательными, но для порядка лучше всего придерживаться единой системы обозначений.

Тесты с набором параметров:
    Тест начинается не со слова "Сценарий: ...", как обычно, а с "Структура сценария: ..."
    Параметры должны быть заключены в кавычки и угловые скобки.

Шаги:
    В настоящий момент можно использовать следующие шаги (в независимости от этапа Дано/Когда/Тогда/И):
  Действия
        пользователь нажимает кнопку "<button_text>"
        пользователь нажимает кнопку 'Домой'
        пользователь нажимает кнопку 'Помощь'
        пользователь нажимает кнопку 'Карандаш'
        пользователь нажимает кнопку 'Операции'  -  отображается при просмотре опреации из раздела История операций
        пользователь нажимает кнопку "<button_text>" на чеке
        пользователь нажимает кнопку 'прокрутка календаря назад
        пользователь нажимает кнопку 'прокрутка календаря вперед'
        пользователь прокручивает страницу вниз "<number>" раз
        пользователь прокручивает страницу вверх "<number>" раз
        пользователь вводит "<text>"
        пользователь нажимает кнопку 'Backspace' "<number>" раз
        пользователь ожидает "<number>" секунд
  Действия для АРМ
        пользователь выбирает строку "<text>"
        установить чек-бокс "<text>"
  Проверки:
        страница 'Платежи и переводы' открыта
        страница 'Главный экран' открыта
        элемент "<element_type>" с текстом "<element_text>"  отображен
        элемент "<element_type>" с текстом "<element_text>"  отображен на странице 'ОК'
        элемент "<element_type>" с текстом "<element_text>"  отображен на странице 'Ошибка'
        элемент "<text_left>" с текстом "<text_right>"  отображен на странице 'Итого'
        элемент с текстом "<element_text>" отображен
        элемент с текстом "<element_text>" не отображен
        элемент 'notes' с текстом "<element_text>"  отображен
        элемент 'поле региона' с текстом "<element_text>"  отображен
        элемент 'support' с текстом ""<element_text>"  отображен
        элемент 'экран ожидания'  отображен
        элемент 'помощь' с текстом "<element_text>"  отображен
        элемент прокрутки страницы  отображен
        шаблон "<template_name>"  отображен
        приветствие "<element_text>"  отображено
        элемент "<element_type>" с текстом "<element_text>" отсутствует
        клавиатура  отображена
        чек  отображен
        на чеке  отображена строка "<text>"
        адресная строка содержит "<text>"

  Проверки лога
        в логе сервера присутствует строка "<text>"
        в логе ПН сервера присутствует строка "<text>"
        в логе клиента присутствует строка "<text>"
        в логе эмулятора присутствует строка "<text>"



        * Пробелы игнорируются
        * Кавычки " " обязательны в шаге, а угловые скобки < > - описывают переменную.
          В шагах, где присутствуют кавычки одиночные ' ' без угловых скобок < > не допускаются другие переменные, например:
              пользователь нажимает кнопку "<button_name>" - означает, что можно ввести название кнопки самостоятельно: пользователь нажимает кнопку "Платежи"
              пользователь нажимает кнопку 'Домой' - означает, что этот шаг не использует переменные, это просто нажатие кнопки "Домой"
          Эти нюансы связаны с неудобными названиями элементов в коде страницы или с отсутствием явной надписи на кнопке, как в случае с "домиком",
          для таких элементов пришлось создать отдельные шаги.

 Начало шагов на русском:
    	  "name": "Russian",
    	  "native": "русский",
    	  "feature": "Функция|Функционал|Свойство",
    	  "background": "Предыстория|Контекст",
    	  "scenario": "Сценарий",
    	  "scenario_outline": "Структура сценария",
    	  "examples": "Примеры",
    	  "given": "*|Допустим|Дано|Пусть",
    	  "when": "*|Если|Когда",
    	  "then": "*|То|Тогда",
    	  "and": "*|И|К тому же|Также",
    	  "but": "*|Но|А"
